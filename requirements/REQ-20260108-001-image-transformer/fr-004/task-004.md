# Task 004: Register IImageProcessor in DI Container

Add the registration of IImageProcessor as a scoped service in the dependency injection setup within Program.cs to enable injection into the processing logic.

Deliverable: Updated Program.cs with the service registration line added.